{
  "name": "drive-ui-checks",
  "private": true,
  "scripts": {
    "test": "node --test tests/ui/*.test.cjs",
    "format": "prettier --write app/static/*.js app/templates/*.html"
  },
  "devDependencies": {
    "jsdom": "26.1.0",
    "prettier": "3.5.3"
  }
}
