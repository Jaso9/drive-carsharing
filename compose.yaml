services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/carsharing.db
    volumes:
      - drive-data:/data
    restart: unless-stopped
volumes:
  drive-data:
